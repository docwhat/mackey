#!/bin/bash

set -eu

cd "$(dirname "$0")"

export GEM_HOME="${PWD}/build-deps"
unset GEM_PATH

/usr/bin/gem install --conservative --no-document bundler

"${GEM_HOME}/bin/bundle" install --standalone --clean --binstubs=bin

# EOF
